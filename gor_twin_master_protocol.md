# GOR & TWIN Master Engineering, Architecture, Security & QA Protocol
# מבוסס על פרויקט TWIN — מחייב בכל פרויקט, אפליקציה ומערכת של GOR MARKETING

---

## 0 · העדפות אישיות קבועות (איגור) — חוקי ברזל מחייבים

| נושא | ההעדפה והפרוטוקול |
|---|---|
| **שפה** | עברית תמיד, כולל ממשק המוצר (RTL מלא) |
| **סגנון עבודה** | מעשי ואיטרטיבי — בונים, בודקים חי, רואים תוצאה, מכווננים |
| **קצב** | כירורגי, אחד-אחד. לא לזרוק גוש שינויים בבת אחת |
| **מחיקות** | **אף פעם לא למחוק פיצ'ר על דעת עצמי.** להציג ממצא + המלצה, ולחכות לאישור |
| **אימות** | 100%, לא הנחות. כל טענה מגובה בבדיקה שרצה בפועל |
| **אחרי שינוי DB** | תמיד להריץ בדיקות SQL שמאמתות את המצב |
| **אחרי פריסה** | בדיקה חיה בדפדפן על הגרסה שבאוויר, לא רק על הקוד |
| **גרסאות** | `<שם-פרויקט> N.zip` — מספר עולה, **שם בסיס לא משתנה לעולם** |
| **פריסה** | Netlify Drop (גרירת תיקייה/zip). לא מחובר ל-GitHub |
| **הרשאות** | ברירת מחדל: **הכול פתוח**. האדמין מכבה נקודתית מה שירצה |
| **דיווח** | תמצית ממצאים → המלצה → שאלה. בלי הצפה |
| **כנות** | אם משהו לא אפשרי (כמו הסתרת קוד פרונט) — להגיד את זה ישר, לא למכור אשליה |

---

## 1 · ארכיטקטורה ומבנה קוד

### 1.1 בחירת ארכיטקטורה
- **אתר תדמית / דף נחיתה** → HTML+CSS+JS סטטי. בלי DB. פריסה ל-CDN.
- **אתר עם תוכן דינמי** → סטטי + Supabase לקריאה בלבד.
- **אפליקציה / מערכת** → Supabase (Postgres + Auth + Storage + Edge) + פרונט סטטי.

### 1.2 כללי מבנה קוד (הסטנדרט המנצח של TWIN)
- **קובץ HTML אחד** לאפליקציה + **JS מפוצל למודולים ממוספרים** (`app-01.js` … `app-08.js`).
- **CSS מרוכז** בקובץ אחד (`fonts.css`) + סגנונות נקודתיים ב-HTML.
- **נתב מרכזי אחד** לניווט (`navTo(id)`) — נקודת חנק אחת לכל בדיקות ההרשאה.
- **פונקציית כניסה אחת** לדשבורד (`enterDashboard(profile)`) — שם יושבים כל השערים (חסימה, אישור).
- **פונקציית תצוגה אחת לפי תפקיד** (`applyRoleVisibility()`) — כל הצגה/הסתרה של רכיבים במקום אחד.
- **מפות תוויות במקום אחד** (`SLB`, `SLC`, `ROLE_LBL`) — הוספת סטטוס חדש = שורה אחת.
- **הערות בעברית בקוד** — תיעוד ברור לכל רכיב.

### 1.3 מודל תפקידים (Roles) וקטלוג יכולות
- טבלת `profiles` עם `role` + מזהי קישור (`client_id`, `building_id`, `resident_id`, `supplier_id`).
- **דגל אדמין-על נפרד** (`is_alpha`) — מאפשר תת-אדמינים עם הרשאות חלקיות.
- **ריבוי כובעים**: טבלת `user_dashboards` — מעבר בין תפקידים למייל בודד.
- **הרשאות גרנולריות** ב-`permissions jsonb` (`null` = הכול מותר).
- **קטלוג יכולות (`ROLE_CAPS`)**: לכל פעולה יש מפתח יציב, תווית וסלקטור DOM אמיתי ומאומת.

---

## 2 · נתונים ואחסון (Supabase & Database Standards)

### 2.1 מסד נתונים
- **RLS מופעל על כל טבלה. בלי יוצא מן הכלל.**
- **פונקציות עזר (`SECURITY DEFINER` + `SET search_path`)** הנגזרות מ-`auth.uid()` בלבד:
  `is_super_admin()`, `my_client_id()`, `my_building_id()`, `my_supplier_id()`, `owns_building(id)`, `has_perm(key)`.
- **טריגר הגנה על `profiles`** למניעת הסלמת הרשאות עצמית.
- **פעולות רגישות רק דרך RPC** עם בדיקת הרשאה בשורה הראשונה.

### 2.2 שינויי סכימה ומלכודות
- **הוספת עמודה עם `NOT NULL DEFAULT false`** → תמיד להוסיף ומיד `UPDATE` לשורות הקיימות (Backfill).
- **`CHECK` constraint** → לעדכן תמיד לפני הוספת סטטוסים חדשים.
- **שינוי טיפוס החזרה** → דורש `DROP FUNCTION` קודם.
- **Realtime** → תמיד להוסיף לפרסום: `alter publication supabase_realtime add table X;`.

### 2.3 Storage (חוקי ברזל)
- **דלי ציבורי = תמונות פרופיל/לוגו/מדיה שיווקית בלבד.**
- **פרטי + Signed URL** לכל מסמך, חוזה, קובץ לקוח, PDF ותמונה פרטית.
- **מדיניות SELECT/DELETE** בודקת בעלות אמיתית (Join לפרופיל/לקוח).
- **לעולם לא לשים מסמכים עסקיים בתיקיית הפריסה (`/docs`).**

---

## 3 · פריסה, אבטחה וכותרות (`_headers`)

### 3.1 תהליך הפריסה
1. עריכה מקומית → 2. בדיקת תחביר (`node --check`) → 3. אריזה `<שם> N.zip` → 4. גרירה ל-Netlify Drop → 5. **בדיקה חיה בדפדפן**.
- סנכרון מלא: שינוי DB ושינוי קוד עולים תמיד יחד.

### 3.2 כותרות אבטחה (`_headers`)
- אכיפת `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`.
- אכיפת CSP מוקפד עם מעבר דרך Report-Only.
- `Cache-Control: no-cache, no-store, must-revalidate` על HTML ו-`sw.js`.
- מניעת הגעת קבצים אסורים לפריסה (`.env`, `.git`, `*.map`, `.bak`, `.sql`, מסמכים עסקיים).

### 3.3 אבטחה ו-Edge Functions
- אין סודות בפרונט — `service_role` בשרת בלבד.
- `verify_jwt: true` כברירת מחדל. אם `false` — אימות פנימי **fail-closed** (לא fail-open).
- אימות חתימות ספק חיצוני ב-Webhooks.
- מקור אמת יחיד לסודות (Supabase Vault / Edge env).
- אימות משתמשים: `approved` + `blocked` kill switch + מחיקה מלאה ב-RPC.

---

## 4 · צ'קליסט השקה ואימות קצה-לקצה
- **לפני:** RLS פעיל בכל הטבלאות · דליים פרטיים · אין סודות בפרונט · `_headers` מעודכן · אפס סלקטורים מתים.
- **בדיקה חיה:** בדיקת משתמש אמיתי בדפדפן + בדיקה שלילית אנונימית אמיתית (`persistSession:false`, storageKey ייחודי) שמקבלת `not authorized`.
- **אחרי:** אפס שגיאות קונסול, ניקוי משתמשי בדיקה, תיעוד הגרסה.
